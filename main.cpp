#include <iostream>
#include <Vector.h>

int main()
{
   /* Vector3 v1(2, 3, 4);
    Vector3 v2(6, 3, 7);
    float dot;

    dot = Vector3::dot(v1, v2);
    std::cout << "The result of the dot product between the v1 and v2 is: " << dot
         << "\n";

         */
}